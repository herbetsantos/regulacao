import { json,logAudit } from '../../_utils.js';
import { requireAdminAccess } from '../../_shared.js';
export async function onRequestPut({request,env,params}){const {user,error}=await requireAdminAccess(request,env);if(error)return error;const id=String(params.id||'');const cur=await env.DB_REGULACAO.prepare('SELECT * FROM regulacao_profissionais WHERE id=?').bind(id).first();if(!cur)return json({error:'Profissional não encontrado.'},404);let b;try{b=await request.json()}catch{return json({error:'JSON inválido.'},400)}const nome=b.nome==null?cur.nome:String(b.nome).trim(),registro=b.registro_profissional===undefined?cur.registro_profissional:(String(b.registro_profissional||'').trim()||null),pid=b.principal_id===undefined?cur.principal_id:(b.principal_id?String(b.principal_id):null),equipe=b.equipe_id===undefined?cur.equipe_id:(b.equipe_id?Number(b.equipe_id):null),ativo=b.ativo===undefined?cur.ativo:(b.ativo?1:0);if(pid){const other=await env.DB_REGULACAO.prepare('SELECT id,nome FROM regulacao_profissionais WHERE principal_id=? AND id<>? AND ativo=1').bind(pid,id).first();if(other)return json({error:`Esta conta já está vinculada a ${other.nome}.`},409)}await env.DB_REGULACAO.prepare("UPDATE regulacao_profissionais SET nome=?,registro_profissional=?,principal_id=?,equipe_id=?,ativo=?,updated_at=datetime('now') WHERE id=?").bind(nome,registro,pid,equipe,ativo,id).run();await logAudit(env,user,'update','regulacao_profissional',id,{nome,pid,equipe,ativo});return json({ok:true})}
