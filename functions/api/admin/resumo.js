import { json } from '../_utils.js';
import { requireAdminAccess } from '../_shared.js';
import { syncLegacyAccessModel,syncLegacyProfessionalModel } from '../_hybrid.js';
export async function onRequestGet({request,env}){const {error}=await requireAdminAccess(request,env);if(error)return error;try{await Promise.all([syncLegacyAccessModel(env),syncLegacyProfessionalModel(env)])}catch{}const q=async(sql)=>Number((await env.DB_REGULACAO.prepare(sql).first())?.n||0);const [locals,prof,semVinc,semHoras,espSemProf,principal]=await Promise.all([q('SELECT COUNT(*) n FROM regulacao_local_users WHERE active=1'),q('SELECT COUNT(*) n FROM regulacao_profissionais WHERE ativo=1'),q(`SELECT COUNT(*) n FROM regulacao_profissionais p WHERE p.ativo=1 AND NOT EXISTS(SELECT 1 FROM regulacao_profissional_vinculos v WHERE v.profissional_id=p.id AND v.ativo=1)`),q(`SELECT COUNT(DISTINCT p.id) n FROM regulacao_profissionais p JOIN regulacao_profissional_vinculos v ON v.profissional_id=p.id AND v.ativo=1 WHERE p.ativo=1 AND v.carga_horaria_semanal<=0`),q(`SELECT COUNT(*) n FROM especialidades e WHERE e.ativo=1 AND NOT EXISTS(SELECT 1 FROM regulacao_profissional_vinculos v JOIN regulacao_profissionais p ON p.id=v.profissional_id AND p.ativo=1 WHERE v.especialidade_id=e.id AND v.ativo=1)`),q('SELECT COUNT(*) n FROM regulacao_principal_acessos WHERE active=1')]);const units=Number((await env.DB.prepare('SELECT COUNT(*) n FROM unidades WHERE ativo=1').first())?.n||0),teams=Number((await env.DB.prepare('SELECT COUNT(*) n FROM regulacao_equipes WHERE ativo=1').first())?.n||0);return json({resumo:{usuarios_externos:locals,usuarios_com_acesso:principal,profissionais:prof,unidades:units,equipes:teams},alertas:{profissionais_sem_vinculo:semVinc,profissionais_sem_horas:semHoras,especialidades_sem_profissional:espSemProf}})}
